interface:
  display_name: "SPD V1 Batch"
  short_description: "High-throughput browser-first directory queue and submission workflow"
  default_prompt: "Use $submit-product-directories-v1-batch to normalize, shard, verify, and process this authorized directory list. Use a real browser for ordinary web forms, use official APIs only when documented, hand off verification challenges, and keep resumable evidence-backed records."
